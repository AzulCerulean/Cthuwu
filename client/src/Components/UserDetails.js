import styled from "styled-components";

const UserDetails = () => {
  return (
    <MainWrapper>
      <h1>User Stuff</h1>
    </MainWrapper>
  );
};

const MainWrapper = styled.div``;

export default UserDetails;
